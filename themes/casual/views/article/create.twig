{{ use('yii\helpers\Html') }}
{# @var $this yii\web\View
 @var $model app\models\Article #}
{{ set(this, 'title', t('Create Articles')) }}
{{ set(this, 'params', {
    'breadcrumbs': [
        {'label': t('Articles'), 'url': ['index']},
        {'label': this.title}
    ]
}) }}
<div class="container ">
    <div class="col-sm-10  col-sm-push-1 box">
        <hr>
        <h2 class="intro-text text-center">{{ this.title }} </h2>
        <hr>
        {{ this.render('_form.twig', {'model': model}) | raw }}
    </div>
</div>
